# argonaut — Cyrius port
# Ported from 13577 lines of Rust

include "lib/string.cyr"
include "lib/fmt.cyr"
include "lib/alloc.cyr"
include "lib/vec.cyr"
include "lib/str.cyr"
include "lib/syscalls.cyr"

fn main() {
    alloc_init();
    println("argonaut ready");
    return 0;
}

var r = main();
syscall(SYS_EXIT, r);
